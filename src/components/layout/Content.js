import { Layout } from "antd";

export default ({}) => {
  return (
    <Layout.Content style={{ padding: "0 50px" }}>
      <div
        style={{
          minHeight: "100%",
          padding: "24px",
          background: "#fff",
        }}
      >
        Content
      </div>
    </Layout.Content>
  );
};
